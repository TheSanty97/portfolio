<header
  class="fixed top-0 left-0 w-full h-25 bg-transparent text-white flex justify-center items-center z-10"
>
  <nav class="flex space-x-4">
    <a
      href="#home"
      data-target="home"
      class="px-4 py-2 rounded-full border-3 border-transparent bg-purple-500 text-white font-semibold hover:bg-blue-700 hover:border-gradient-to-r hover:from-blue-700 hover:to-purple-500 transition duration-300"
    >
      Inicio
    </a>
    <a
      href="#about"
      data-target="about"
      class="px-4 py-2 rounded-full border-3 border-transparent bg-purple-500 text-white font-semibold hover:bg-blue-700 hover:border-gradient-to-r hover:from-blue-700 hover:to-purple-500 transition duration-300"
    >
      Sobre mí
    </a>
    <a
      href="#projects"
      data-target="projects"
      class="px-4 py-2 rounded-full border-3 border-transparent bg-purple-500 text-white font-semibold hover:bg-blue-700 hover:border-gradient-to-r hover:from-blue-700 hover:to-purple-500 transition duration-300"
    >
      Proyectos
    </a>
    <a
      href="#contact"
      data-target="contact"
      class="px-4 py-2 rounded-full border-3 border-transparent bg-purple-500 text-white font-semibold hover:bg-blue-700 hover:border-gradient-to-r hover:from-blue-700 hover:to-purple-500 transition duration-300"
    >
      Contacto
    </a>
    <a
      href="#rickandmorty"
      data-target="rickandmorty"
      class="px-4 py-2 rounded-full border-3 border-transparent bg-purple-500 text-white font-semibold hover:bg-blue-700 hover:border-gradient-to-r hover:from-blue-700 hover:to-purple-500 transition duration-300"
    >
      Rick and Morty
    </a>
  </nav>
</header>

<!-- <script>
  document.querySelectorAll("header nav a").forEach((link) => {
    link.addEventListener("click", (e) => {
      e.preventDefault();
      const targetId = link.getAttribute("data-target"); // usamos data-target
      if (!targetId) return;

      const targetSection = document.getElementById(targetId);
      if (targetSection) {
        // Scroll suave
        targetSection.scrollIntoView({ behavior: "smooth" });

        // Cambiar la URL a /home, /about, etc.
        history.pushState(null, "", `/${targetId}`);
      }
    });
  });
</script> -->
